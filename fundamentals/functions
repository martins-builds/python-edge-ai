def message():
    print("Enter a value: ")

def subtra(a, b):
    print(a - b)

def strange_list_fun(n):
    strange_list = []
    
    for i in range(0, n):
        strange_list.insert(0, i)
    
    return strange_list

def not_strange_list_fun(n):
    strange_list = []
    
    for i in range(0, n):
        strange_list.insert(0, i)
    
    return strange_list

print(not_strange_list_fun(5))
message()

subtra(5, 2)
subtra(2, 5)